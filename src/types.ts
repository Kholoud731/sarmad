export type PostData = {
  fname: string;
  mname: string;
  lname: string;
  nat: string;
};

export type ResultRecordDetails = {
  id: string;
  name: string;
  des: string;
  birth: string;
  score: number;
  nat: string;
};
